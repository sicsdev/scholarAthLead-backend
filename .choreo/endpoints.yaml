endpoints:
  - name: my-nodejs-app
    path: /
    http_methods:
      - GET
      - POST
    scripts:
      pre_script: npm install  # Example of a script to run before starting the server
      start_script: npm start  # Command to start your Node.js application
    buildpacks:
      - type: nodejs
        version: 18.17  
